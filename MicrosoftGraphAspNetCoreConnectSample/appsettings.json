{
  "AzureAd": {
        "Instance": "https://login.microsoftonline.com/",
        "CallbackPath": "/signin-oidc",
        "BaseUrl": "https://localhost:44334",
    "ClientId": "ENTER_YOUR_APP_ID",
    "ClientSecret": "ENTER_YOUR_SECRET", // This sample uses a password (secret) to authenticate. Production apps should use a certificate.
        "TenantId": "ENTER-YOUR-TENANT-ID",
        "Scopes": "openid profile offline_access",
        "GraphResourceId": "https://graph.microsoft.com/",
        "GraphScopes": "User.Read User.ReadBasic.All Directory.AccessAsUser.All"
    },
  "Logging": {
    "IncludeScopes": false,
    "LogLevel": {
      "Default": "Warning"
    }
  }
}